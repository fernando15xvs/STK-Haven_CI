name: gym_tracker_web
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  supabase_flutter: ^2.17.2
  core:
    path: ../../packages/core
  csv: ^8.0.0
  file_picker: ^12.0.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
